{
  "name": "wa-bot",
  "version": "1.3.4",
  "description": "TODO",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node dist/index.js",
    "watch": "nodemon --watch src --ext ts --exec ts-node src/index.ts",
    "build": "tsc",
    "prettier": "prettier --write \"src/**/*.ts\"",
    "pm2-reset": "npm run prettier && npm run reset && npm run build && pm2 delete wa-bot || true && pm2 start ecosystem.config.js && pm2 save",
    "pm2": "npm run prettier && npm run build && pm2 delete wa-bot || true && pm2 start ecosystem.config.js && pm2 save",
    "clear-logs": "rm -rf logs/*.log*",
    "reset": "rm -rf .wwebjs_auth .wwebjs_cache data/*.json logs core dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shyland-dev/WA-Bot.git"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "wa"
  ],
  "author": "shyland-dev",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/shyland-dev/WA-Bot/issues"
  },
  "homepage": "https://github.com/shyland-dev/WA-Bot#readme",
  "dependencies": {
    "axios": "^1.12.2",
    "body-parser": "^2.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0",
    "uuid": "^13.0.0",
    "whatsapp-web.js": "^1.34.2"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.5",
    "@types/node": "^24.3.0",
    "@types/qrcode": "^1.5.5",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/uuid": "^10.0.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
